name: Test

on:
    push:
        branches:
            - main

jobs:
    test:
        runs-on: self-hosted
        steps:
            - name: Checkout
              uses: actions/checkout@v6

            - name: vlan creation
              run: |
                vlan 10
                description:Vlan 10 created
                exit
                vlan 20
                description:Vlan 20 cp_mgmt_threat_protection_override:
                exit
                